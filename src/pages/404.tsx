import React from "react"

const NotFoundPage = () => {
  return (
  <div className="notFound">
    <h1>А, ой...</h1>
    <p>Так получилось, что такой страницы не существует, давайте <a href="/" className="notFound_link">вернемся на главную?</a></p>    
  </div>
);}

export default NotFoundPage
